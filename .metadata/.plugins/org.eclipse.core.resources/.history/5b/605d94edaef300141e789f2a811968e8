/*
 * exer.cpp
 *
 *  Created on: 2015. 5. 2.
 *      Author: root
 */

#include <iostream>
#include <cstdlib>
#include <string>

using namespace std;

const int SIZE = 10;

int main() {

	double saveMoney[SIZE];
	string str;

	for (int i = 0; i < SIZE; i++) {
		cout << "Input #" << i + 1 << "Money: ";
		cin >> str;

		if (isalpha(saveMoney[i]))
			exit(EXIT_FAILURE);
		cout << endl;
	}

	return 0;

}
