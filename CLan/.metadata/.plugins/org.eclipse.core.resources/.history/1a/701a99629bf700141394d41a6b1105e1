/*
 * M02-1.c
 *
 *  Created on: 2015. 5. 11.
 *      Author: root
 */

#include <stdio.h>

#define MUL(x, y) printf(""#x" * "#y" = %d\n", x * y)
#define ADD(x, y) printf(""#x" + "#y" = %d\n", x + y)

int main(void) {

	int x, y;
	scanf("%d %d", &x, &y);
	MUL(x, y);
	ADD(x, y);

	return 0;
}
