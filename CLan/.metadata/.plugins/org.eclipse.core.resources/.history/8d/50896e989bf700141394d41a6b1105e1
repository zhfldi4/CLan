/*
 * M02-1.c
 *
 *  Created on: 2015. 5. 11.
 *      Author: root
 */

#include <stdio.h>

#define MUL(x, y) printf("%d * %d = %d\n", x, y, x * y)
#define ADD(x, y) printf("%d + %d = %d\n", x, y, x + y)

int main(void) {

	int x, y;
	scanf("%d %d", &x, &y);
	MUL(x, y);
	ADD(x, y);

	return 0;
}
